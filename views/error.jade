extends layout.jade

block content
  h1= message
  if error.status
    h2= error.status
  // Error details are handled by app.js error handlers
  // Stack traces are only shown in development mode server-side
